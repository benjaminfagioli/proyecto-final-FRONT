import React from "react";
import { BrowserRouter, Routes } from "react-router-dom";

const routes = () => {
  return (
    <>
      <BrowserRouter>
        <Routes></Routes>
      </BrowserRouter>
    </>
  );
};

export default routes;
